{
  "editor.formatOnSave": true,
  "emeraldwalk.runonsave": {
    "commands": [
      {
        "match": "\\.py$",
        "cmd": "autoflake --in-place --remove-all-unused-imports ${file} && isort ${file} && black ${file}"
      }
    ]
  }
}